{
  "name": "mcscafe",
  "version": "1.0.0",
  "private": true,
  "description": "Site estático demonstrativo do MCS Café.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node scripts/check-project.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
